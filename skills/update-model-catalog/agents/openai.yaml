interface:
  display_name: "Update Model Catalog"
  short_description: "Safely update and publish Sub2API model metadata"
  default_prompt: "Use $update-model-catalog to update, validate, publish, and verify the Sub2API model catalog."
