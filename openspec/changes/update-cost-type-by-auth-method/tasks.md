## 1. Implementation

- [x] 1.1 新增 `account_subscription_cycles` 迁移、读写模型和周期管理 API；移除未发布的单一 `period_start_at` 方案。
- [x] 1.2 在利润服务中集中实现认证类型到成本口径的映射，并移除成本配置对口径的覆盖。
- [x] 1.3 实现不连续订阅周期成本归属：只计算查询范围与每条周期账本记录的交集，空档成本为 0。
- [x] 1.4 调整成本配置 API，使 API Key 账号不再要求或使用订阅配置；订阅账号改为管理周期账本。
- [x] 1.5 调整成本配置弹窗：显示自动判定结果，并为订阅账号提供周期记录列表与新增充值周期入口。
- [x] 1.6 为订阅周期录入提供一键日期推算：展示来源与未确认状态，禁止自动写入或参与利润计算。
- [x] 1.7 调整利润列表标签与本周期回本卡，显示当前有效周期、最近周期或“未订阅”。
- [x] 1.8 调整按量账号利润展示，明确上游采购折扣与客户销售倍率；成本使用历史账号倍率快照。
- [x] 1.9 移除窗口基准收入手填项，改为计算历史连续 5 小时最高实际收入；历史不足时隐藏效率。
- [x] 1.10 增加服务层和前端回归测试，覆盖 OAuth、Setup Token、API Key、采购折扣、两段不连续周期、费用变更、日期推算、自动窗口基准与缺失周期。
- [x] 1.11 运行 Go 定向测试、前端类型检查和生产构建。
- [x] 1.12 将账号级经营统计收敛到账号管理的用量详情抽屉，利润页仅保留全局汇总与趋势。
- [x] 1.13 订阅账号抽屉显示固定本期采购成本与本期现金利润；全局范围报表明确标注期间摊销成本。
- [x] 1.14 在全局利润页趋势下方恢复只读账号明细，并与日期范围同步，保留账号配置在账号管理抽屉中。
- [x] 1.15 允许零成本订阅周期作为有效周期，返回成本 `$0` 与对应周期利润，避免误报未录入。
- [x] 1.16 增加按账号、按日的利润基础数据批量聚合，一次扫描同时提供汇总与趋势所需收入和 API Key 按量成本。
- [x] 1.17 批量读取订阅周期、窗口基准与当前周期收入，消除 summary/trend 随订阅账号数量增长的 N+1 查询。
- [x] 1.18 新增利润 overview 聚合接口并让利润页改为单请求加载；保留 summary/trend 兼容接口。
- [x] 1.19 将批量订阅配置改为单次批量写入，保留跳过已配置账号的语义和原子性。
- [x] 1.20 增加数据库查询预算、结果等价性与前端单请求回归测试，并记录优化前后基准。
- [x] 1.21 将利润 overview 的会计数据加载拆为轻量模式，跳过账号抽屉专用的最佳窗口和当前周期收入聚合。
- [x] 1.22 为利润 overview 增加 5 分钟有界快照缓存、singleflight 回源合并、ETag、缓存状态与生成时间。
- [x] 1.23 在成本配置和订阅周期写入成功后失效利润快照，并支持管理员手动刷新绕过缓存。
- [x] 1.24 限制利润快照缓存键数量并清理过期项，避免任意日期范围造成无界内存增长。
- [x] 1.25 增加轻量查询预算、缓存命中/失效/并发/容量与前端快照提示回归测试，并记录冷缓存和热缓存耗时。
- [x] 1.26 在 Grok 成本配置界面明确区分付款周期和自然月额度重置，禁止将额度 `billing_period_end` 用作财务周期推算来源。
- [x] 1.27 增加 Grok 跨月额度重置回归测试：7/17—8/17 仍为单一采购周期，8/1 不新增或重复摊销成本。

## 2. Rollout

- [ ] 2.1 在生产数据库只读列出认证类型与旧成本配置不一致的账号。
- [ ] 2.2 部署后验证利润摘要与账号配置界面。
